import numpy as np

# Question 1 - The number of iterations it takes gauss-seidel to converge:

def gaus_s (A, B, initial, tol, max_i)
n= len(b)


tol= 1e-6
A = np.array([[3, 1, 3],
              [1, 4, 1],
              [2, 3, 7]])
B = np.array([1, 3, 0])
